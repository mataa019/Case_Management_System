package com.example.casemanagementsystem.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class FlowableConfig {
    // Default config is usually enough unless you want custom behavior.
}